import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.pcpartpicker',
  appName: 'pcpartpicker',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
